import 'package:flutter/material.dart';

class CalculaterServices extends StatelessWidget {
  const CalculaterServices();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: SizedBox(
        child: Text("Calculater Services page"),
      ),
    );
  }
}
