import 'package:flutter/material.dart';

class DevciesList extends StatelessWidget {
  const DevciesList();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: SizedBox(
        child: Text("Devices list page"),
      ),
    );
  }
}
